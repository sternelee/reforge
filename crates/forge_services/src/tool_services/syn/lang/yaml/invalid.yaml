# Invalid YAML with syntax errors
app:
  name: "ForgeCode"
  version: "1.0.0"
  environment: "production"
  
database:
  host: "localhost"
  port: 5432
  name: "forgedb"
  credentials:
    username: "forge_user"
    password: "${DB_PASSWORD}"
  
features:
  - name: "syntax_validation"
    enabled: true
    config:
      timeout: 30
      languages: ["rust", "python", "javascript"
  
  - name: "code_completion"
    enabled: false
    config:
      timeout: 15
      
servers:
  api:
    host: "api.forgecode.dev"
    port: 443
    ssl: true
  database:
    host: "db.forgecode.dev"
    port: 5432
    ssl: false
    invalid_unmatched: [unclosed array